{
  "name": "@pulse/bot",
  "version": "2.0.0",
  "description": ".pulse — a professional, universal Discord moderation and management bot.",
  "type": "module",
  "main": "src/core/bootstrap.js",
  "scripts": {
    "start": "node src/core/bootstrap.js",
    "dev": "node --watch src/core/bootstrap.js",
    "deploy": "node src/core/deploy.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "lint": "eslint src",
    "test": "node --test src/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "eslint": "^8.57.0"
  }
}
